import 'package:flutter/material.dart';

class NavigationRail extends StatelessWidget {
  const NavigationRail({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
